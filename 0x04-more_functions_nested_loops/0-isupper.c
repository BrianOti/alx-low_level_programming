#include <stdio.h>
#include "main.h"

/**
 * _isupper - checks if letter is capitalised or not
 * @c: int representing a character
 * Return: 0
 */

int _isupper(int c)
{
	if (c isupper())
	{
		return (1);
	}
	return (0);
}
